# Cambio — Configuración .env y carga automática (2025-11-05 15:51)

## Alcance
- Creación del archivo `.env` con `DJANGO_SECRET_KEY`, `DJANGO_DEBUG` y `DJANGO_ALLOWED_HOSTS` para ejecutar la aplicación en modo producción/despliegue.
- Actualización de `scripts/start.sh` y `scripts/run.sh` para que automaticen la carga de `.env` antes de aplicar valores por defecto.

## Archivos relevantes
- `.env`
- `scripts/start.sh`
- `scripts/run.sh`

## Próximos pasos
- Mantener la clave secreta fuera del control de versiones (ya ignorada por `.gitignore`).
- Ajustar valores adicionales en `.env` (p.ej. base de datos, credenciales) según el entorno de despliegue.
