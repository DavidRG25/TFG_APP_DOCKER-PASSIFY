version: '3.8'
services:
web:
    build: ./web
    ports:
      - "5000"
    environment:
      - REDIS_HOST=db
    depends_on:
      - db
  db:
    image: redis:alpine
    volumes:
      - redis_data:/data
volumes:
  redis_data:
